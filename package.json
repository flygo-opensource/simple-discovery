{
  "name": "simple-discovery",
  "private": true,
  "packageManager": "bun@1.4.2",
  "description": "Discovery transports sharing one signed Ohayo envelope, several ways to deliver it",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@simple-discovery/*' build",
    "test": "bun run --filter '@simple-discovery/*' test"
  },
  "license": "MIT"
}
