{
  "name": "simple-discovery",
  "private": true,
  "packageManager": "bun@1.4.2",
  "description": "Discovery transports sharing one signed discovery envelope, several ways to deliver it",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@simple-discovery/*' build",
    "test": "bun run --filter '@simple-discovery/*' test"
  },
  "license": "MIT",
  "devDependencies": {
    "@simple-discovery/core": "workspace:*",
    "tsup": "^8.5.1"
  }
}
